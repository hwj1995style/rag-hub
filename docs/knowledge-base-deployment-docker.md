# rag-hub Docker ????

## 1. ????

??????? Docker / Docker Compose ?? `rag-hub`?????

- Linux Docker Engine
- Windows Docker Desktop
- macOS Docker Desktop

?????? Host Linux ????????????????????

## 2. ????????

### 2.1 ??????

```bash
cp deploy/docker/.env.example deploy/docker/.env
```

????? MySQL ????? `13306 -> 3306`????????????? `3306` ???????????

### 2.2 ?? backend ?

?????????

- `backend/target/rag-hub-backend-0.0.1-SNAPSHOT.jar`

### 2.3 ????

```bash
docker compose -f deploy/docker/docker-compose.yml --env-file deploy/docker/.env up -d
```

### 2.4 ????

```bash
docker compose -f deploy/docker/docker-compose.yml --env-file deploy/docker/.env ps
```

### 2.5 ????

```bash
docker compose -f deploy/docker/docker-compose.yml --env-file deploy/docker/.env down
```

## 3. ????

Docker ?????? bootstrap admin?????? JWT ??????????

```env
KB_JWT_ISSUER=rag-hub
KB_JWT_SECRET=replace-with-a-strong-secret
KB_JWT_EXPIRATION_MINUTES=120
KB_BOOTSTRAP_ADMIN_ENABLED=false
```

???????????????????

```env
KB_BOOTSTRAP_ADMIN_ENABLED=true
KB_BOOTSTRAP_ADMIN_USERNAME=admin
KB_BOOTSTRAP_ADMIN_PASSWORD=ChangeMe123!
KB_BOOTSTRAP_ADMIN_DISPLAY_NAME=Bootstrap Admin
KB_BOOTSTRAP_ADMIN_ROLE=admin
```

??????????? bootstrap admin??????????

## 4. ??????

?? Compose ?????????

- `mysql healthy -> rag-hub-backend`
- `mysql healthy -> rag-hub-parser-worker`
- `rag-hub-backend healthy -> nginx`

?????? backend ? nginx ????????????????

## 5. ????

???????

- `GET /actuator/health`
- `POST /api/auth/login`
- ?? Bearer Token ?? `GET /api/documents`
- ?? admin Token ?????????

## 6. ????

- [Docker ????](D:/Projects/rag-hub/deploy/docker/README.md)
- [????](D:/Projects/rag-hub/docs/knowledge-base-authentication.md)
- [????](D:/Projects/rag-hub/docs/knowledge-base-ops-handbook.md)
