# rag-hub ???????

## 1. ??????

?????????????????????????

- Spring Security ??
- ??????
- Bearer JWT ?????
- ???????
- ????????? `admin` ???

???????

- ???????????????????????
- refresh token
- ?????
- ??????

## 2. ????

???`POST /api/auth/login`

????

```json
{
  "username": "admin",
  "password": "ChangeMe123!"
}
```

?????? `data` ???

```json
{
  "tokenType": "Bearer",
  "accessToken": "<jwt>",
  "expiresInSeconds": 7200,
  "user": {
    "userId": "uuid",
    "username": "admin",
    "displayName": "Local Admin",
    "roleCode": "admin"
  }
}
```

?????

- ????????`400 / KB-40001`
- ?????????`401 / KB-40102`
- ????? `active`?`401 / KB-40102`

## 3. Token ????

??????????????

```http
Authorization: Bearer <jwt>
```

???????

- `POST /api/auth/login`
- `GET /actuator/health`
- `GET /actuator/info`
- `/h2-console/**` ?????????

?? `/api/**` ????????

## 4. ??????

???????? `kb_admin_user.role_code`?

??? `admin` ????????

- `POST /api/documents/upload`
- `POST /api/documents/batch-import`
- `POST /api/documents/{documentId}/reparse`
- `POST /api/documents/{documentId}/versions/{versionId}/activate`
- `POST /api/permissions/bind`

?????????????????

- `GET /api/documents`
- `GET /api/documents/{documentId}`
- `GET /api/documents/{documentId}/chunks`
- `GET /api/tasks/{taskId}`
- `POST /api/search/query`
- `POST /api/qa/query`
- `GET /api/query-logs/{logId}`

## 5. ???

?????`kb.security`

### 5.1 JWT ??

- `kb.security.jwt.issuer`
- `kb.security.jwt.secret`
- `kb.security.jwt.expiration-minutes`

???

- ?????????? `secret`
- `secret` ???? 32 ??
- ????????????

### 5.2 Bootstrap Admin ??

- `kb.security.bootstrap-admin.enabled`
- `kb.security.bootstrap-admin.username`
- `kb.security.bootstrap-admin.password`
- `kb.security.bootstrap-admin.display-name`
- `kb.security.bootstrap-admin.role-code`

?????

- ? `enabled=true` ?????????????????????????????????
- ???? `application.yml` ?? bootstrap admin???????
- `application-prod.yml` ? `application-docker.yml` ???? bootstrap admin???????????????

## 6. Docker ???????

Docker ?????????????

```env
KB_JWT_ISSUER=rag-hub
KB_JWT_SECRET=replace-with-a-strong-secret
KB_JWT_EXPIRATION_MINUTES=120
KB_BOOTSTRAP_ADMIN_ENABLED=false
```

????????????????????

```env
KB_BOOTSTRAP_ADMIN_ENABLED=true
KB_BOOTSTRAP_ADMIN_USERNAME=admin
KB_BOOTSTRAP_ADMIN_PASSWORD=ChangeMe123!
KB_BOOTSTRAP_ADMIN_DISPLAY_NAME=Bootstrap Admin
KB_BOOTSTRAP_ADMIN_ROLE=admin
```

??????????????????

## 7. ????

???????????

- `tester / test123456`??? `admin`
- `viewer / viewer123`??? `viewer`

??????`backend/src/test/java/com/example/kb/KnowledgeBaseApiIntegrationTest.java`

## 8. ????

??????? `kb_permission_policy` ?????????????????????????????????????????

1. ?????chunk ???????? allow/deny ??
2. ??????????????????
