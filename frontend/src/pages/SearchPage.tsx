import { SearchOutlined } from '@ant-design/icons';
import { useMutation } from '@tanstack/react-query';
import { App, Button, Card, Col, Form, Input, InputNumber, Row, Space, Table, Tag, Typography } from 'antd';
import { searchKnowledge } from '../services/api/search';
import type { SearchItem } from '../types/api';
export function SearchPage() { const { message } = App.useApp(); const [form] = Form.useForm(); const mutation = useMutation({ mutationFn: searchKnowledge, onError: (error: Error) => void message.error(error.message) }); return (<div className="content-stack"><Card className="page-card"><Row gutter={[20, 20]}><Col xs={24} xl={10}><Typography.Title level={3} style={{ marginTop: 0 }}>Search Workbench</Typography.Title><Typography.Paragraph type="secondary">This page is wired to POST /api/search/query so we can validate topK and lightweight filters end to end.</Typography.Paragraph><Form form={form} layout="vertical" initialValues={{ topK: 10 }} onFinish={(values) => mutation.mutate({ query: values.query, topK: values.topK, filters: values.bizDomain ? { bizDomain: values.bizDomain } : undefined })}><Form.Item name="query" label="Query" rules={[{ required: true }]}><Input.TextArea rows={4} placeholder="business license" /></Form.Item><Space align="start" wrap><Form.Item name="topK" label="TopK"><InputNumber min={1} max={20} /></Form.Item><Form.Item name="bizDomain" label="Domain filter"><Input placeholder="credit" /></Form.Item></Space><Button type="primary" htmlType="submit" icon={<SearchOutlined />} loading={mutation.isPending}>Search</Button></Form></Col><Col xs={24} xl={14}><div className="metric-grid"><div className="metric-card"><Typography.Text type="secondary">Hits</Typography.Text><Typography.Title level={3}>{mutation.data?.total ?? 0}</Typography.Title></div><div className="metric-card"><Typography.Text type="secondary">State</Typography.Text><Tag color={mutation.data ? 'green' : 'default'}>{mutation.isPending ? 'Running' : mutation.data ? 'Complete' : 'Idle'}</Tag></div></div></Col></Row></Card><Card className="page-card" title="Search Results"><Table<SearchItem> rowKey="chunkId" dataSource={mutation.data?.items ?? []} pagination={false} columns={[{ title: 'Document', dataIndex: 'documentTitle', key: 'documentTitle' }, { title: 'Title Path', dataIndex: 'titlePath', key: 'titlePath' }, { title: 'Locator', dataIndex: 'locator', key: 'locator', width: 100 }, { title: 'Score', dataIndex: 'score', key: 'score', width: 100 }, { title: 'Snippet', dataIndex: 'snippet', key: 'snippet' }]} /></Card></div>); }
