# Docker ????

## 1. ??

????? `rag-hub` ???? Docker ?????

?????

- Linux Docker Engine
- macOS Docker Desktop
- Windows Docker Desktop

## 2. ????

- `docker-compose.yml`
- `.env.example`
- `nginx.conf`

## 3. ????

?? compose ????

- MySQL
- Redis
- MinIO
- Elasticsearch
- Qdrant
- rag-hub-backend
- rag-hub-parser-worker
- nginx

## 4. ????

### 4.1 ??????

```bash
cp deploy/docker/.env.example deploy/docker/.env
```

????????????????? MySQL ?????? `13306`?

### 4.2 ?? backend ?

compose ?????

- `backend/target/rag-hub-backend-0.0.1-SNAPSHOT.jar`

### 4.3 ????

```bash
docker compose -f deploy/docker/docker-compose.yml --env-file deploy/docker/.env up -d
```

### 4.4 ????

```bash
docker compose -f deploy/docker/docker-compose.yml --env-file deploy/docker/.env ps
```

### 4.5 ????

```bash
docker compose -f deploy/docker/docker-compose.yml --env-file deploy/docker/.env down
```

## 5. ??????

???????

- `KB_JWT_ISSUER`
- `KB_JWT_SECRET`
- `KB_JWT_EXPIRATION_MINUTES`
- `KB_BOOTSTRAP_ADMIN_ENABLED`

?? Docker ??? `KB_BOOTSTRAP_ADMIN_ENABLED=false`?????????????????????????????? bootstrap admin?

## 6. ??

- `docker-compose.yml` ?? `.env` ?????
- Docker ???? `deploy/docker/nginx.conf`???? Host Linux ? Nginx ??
- ????? MySQL ? backend ??????? nginx ?? backend healthy ???
- ?? `parser-worker` ???????????????????? backend ?????
